{% extends get_admin_template('base_list_field', admin.code) %}

{% block field %}
    {% if object is instance_of('App\\Entity\\PreRegister') %}
        {% if not object.enabled %}
            <input type="checkbox" name="idx[]" value="{{ admin.id(object) }}" />
        {% endif %}
    {% else %}
        <input type="checkbox" name="idx[]" value="{{ admin.id(object) }}" />
    {% endif %}
{% endblock %}
