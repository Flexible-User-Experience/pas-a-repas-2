{% extends "logins/base.html.twig" %}

{% block title %}{{ controller_name }} {{ parent() }}{% endblock %}

{% block breadcrumb %}
    <li class="breadcrumb-item active" aria-current="page">{{ controller_name }}</li>
{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h1 class="display-6 mb-5">✅ {{ controller_name }}!</h1>
            </div>
        </div>
        {% if auth_error is not null %}
            <div class="row">
                <div class="col-12">
                    <div class="alert alert-primary" role="alert">
                        <i class="fas fa-warning"></i> {{ auth_error}}
                    </div>
                </div>
            </div>
        {% endif %}
    </div>
{% endblock %}

{% block javascripts_body %}
    <script>
        jQuery(document).ready(function() {
            console.log('templates/logins/main_test_login.html.twig on Ready trigger');
        });
    </script>
{% endblock %}
