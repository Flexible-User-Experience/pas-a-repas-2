{% if not object.student.isPaymentExempt and not is_receipt_invoiced(object) %}
    <a class="btn btn-sm btn-default" href="{{ admin.generateObjectUrl('createInvoice', object) }}" title="{{ 'backend.admin.receipt.createInvoice' | trans }}">
        <i class="far fa-clone"></i>
    </a>
{% endif %}
