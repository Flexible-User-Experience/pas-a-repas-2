{% if object.mainSubject.payment != constant('App\\Enum\\StudentPaymentEnum::BANK_ACCOUNT_NUMBER') and not object.student.isPaymentExempt and object.mainEmail %}
    <a class="btn btn-sm btn-default" href="{{ admin.generateObjectUrl('sendReminder', object) }}" title="{{ 'backend.admin.receipt_reminder.list_button' | trans }}">
        <i class="far fa-envelope"></i>
    </a>
{% endif %}
