framework:
    secret: '%env(APP_SECRET)%'
    templating:
        engines: [ 'twig' ]
    session:
        handler_id: null
        cookie_secure: auto
        cookie_samesite: lax
    php_errors:
        log: true
